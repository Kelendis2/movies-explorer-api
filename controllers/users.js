// const user = require('../models/user');
const User = require('../models/user');

const getUser =(req, res) => {
  console.log('Запрос есть');
};

const updateUser = (req, res) => {
  console.log('Запрос есть');
};

module.exports = {
  getUser,
  updateUser,
};
